env:
  FLAG:
    required: true
    type:
      boolean: {}
on:
  workflow_call:
    inputs:
      environment:
        required: true
        type:
          string:
            enum: [staging, production]

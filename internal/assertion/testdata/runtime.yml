env:
  NAME:
    required: true
    type:
      string:
        enum: [staging, production]
        pattern: '^[a-z]+$'
  COUNT:
    required: false
    type:
      integer:
        min: 0
        max: 5
  FLAG:
    type:
      boolean: {}

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
      retries:
        type: number
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo test

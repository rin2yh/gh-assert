inputs:
  environment:
    required: true
    type:
      string:
        enum: [staging, production]
  retries:
    required: false
    type:
      integer:
        min: 0
        max: 5
  dry-run:
    type:
      boolean: {}
